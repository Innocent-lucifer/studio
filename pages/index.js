// This file is intentionally left without a default export of a React component
// to prevent it from being treated as a route. This resolves the conflict with
// the App Router's root page at `src/app/page.tsx`.
