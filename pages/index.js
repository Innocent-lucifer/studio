// This page is disabled because the App Router at /src/app/page.tsx now handles the homepage route.
// Removing the default export prevents Next.js from treating this as a page and resolves the routing conflict.
export {};
